# 開發工作日誌（WORKLOG）

> 內部開發日記：逐日流水帳，記每天做了什麼、當下決策、踩雷、狀態與待辦。
> 反向時間序（最新在上）。每條目五欄位：做了什麼 / 決策 / 踩雷 / 狀態 / 待辦。

---

## 2026-06-09 — 規劃下一步 + RAG 世界觀設計

- **做了什麼**
  - 盤點現況，規劃 v1 之後方向；討論三條路：換本地 TTS、自己訓練聲線、增加角色世界觀。
  - 選定先做「**RE:0 世界觀（RAG）**」，用 brainstorming skill 討論成設計。
  - 探索 OLV 原始碼：`basic_memory_agent` 只有簡單對話記憶、無內建 RAG；system prompt 為 session 靜態；有 MCP 機制；app/ 為 gitignore。
  - 建立並逐步擴充 `WORKLOG.md`。
- **決策**
  - 成功標準＝**對話自然帶設定、不出戲**（氛圍 > 考據）。
  - 知識來源＝**整本小說**，**只存本地、gitignore、不進公開 repo**；repo 只放程式 + README。
  - 架構＝**RAG 代理 Proxy**（夾 OLV ↔ Ollama，攔 `/v1/chat/completions` 檢索注入、串流轉發），不改 OLV 原碼。
  - 選型＝Ollama `bge-m3` + numpy cosine、相似度門檻 gating、容錯退化為不注入。
- **踩雷** —
- **狀態** RAG 設計草案已提，**等使用者拍板** → 接著寫 spec（brainstorming 暫停此步）。
- **待辦** 拍板設計；提供小說 txt 放 `rag/corpus/`；之後寫 spec → 實作 ingest/retriever/proxy。

---

## 2026-06-02 — 專案成果報告 REPORT.md

- **做了什麼** 撰寫 `REPORT.md`（繁中、13 節）；擴充 §6 微調白話、§6.8 emotion 學習、§6.9 訓練前後對比、§8.5 內部執行細節（照 OLV 原碼拆 8 階段）。
- **決策** 數字一律查證（超參讀 train script、版本對齊環境）；無 eval loss 確切值不編；前後對比 base 欄標「示意」。
- **踩雷** —
- **狀態** REPORT 完成、已 commit（未 push）。
- **待辦** 需要時 push；可選做 base 模型同題 A/B 側錄。

---

## 2026-06-01 — VTuber TTS：菜月昴開口（zh-TW + persona）

- **做了什麼** edge_tts voice 改 `zh-TW-YunJheNeural`；瀏覽器聽到繁中男聲 + mao_pro 表情隨語音切換；persona_prompt 定稿「熱血中二吐槽版」。
- **決策** TTS 沿用 edge-tts（輕量、不耗 GPU）；persona 蓋過 Modelfile SYSTEM，改這裡即改實際人設。
- **踩雷** **TTS 無聲真因＝缺 ffmpeg**（pydub 轉 mp3→wav 需要）→ `winget install Gyan.FFmpeg` + 帶 ffmpeg PATH 重啟；表情綁音頻播放（無 TTS 不觸發）。
- **狀態** TTS + 表情驗收通過。
- **待辦** ASR 實測、第 ⑧ 步驗收。

---

## 2026-06-01 — VTuber 階段二：Live2D emotion 管線

- **做了什麼** 驗 emotion 後端管線（LLM 出 `[joy]` → extract_emotion → server 送 `expressions:[3]` → 前端收到）；改用內建 mao_pro。
- **決策** avatar 暫用 mao_pro。
- **踩雷** chitose 是 **Cubism 2.x，前端只支援 3–5** 不相容；表情依賴音頻播放 → 階段順序應 LLM→TTS→表情一起驗。
- **狀態** 後端管線驗通；表情待 TTS 一起驗。
- **待辦** 接 TTS。

---

## 2026-06-01 — VTuber 階段一：文字對話

- **做了什麼** clone OLV 到 app/、`uv sync`、補 git submodule 前端；conf.yaml 指向 qwen3-486；內建網頁文字對話成功。
- **決策** LLM provider 用 ollama_llm、model qwen3-486。
- **踩雷** 預設 persona 是簡體「Mili」覆寫 SYSTEM → 回簡體且非角色 → 改菜月昴 + 禁簡體；前端空白因 submodule 沒拉。
- **狀態** 文字對話通（繁中、菜月昴）。
- **待辦** Live2D 表情。

---

## 2026-06-01 — 第 ⑥ 步：Ollama 部署

- **做了什麼** `ollama create qwen3-486 -f deploy/Modelfile`；API `/v1/chat/completions` 實測回繁中、2–4 句、帶合理 `[emotion]`。
- **決策** Modelfile 未改（emotion 驗證即過）。
- **踩雷** PowerShell `Invoke-RestMethod` 把 UTF-8 body 當 latin1 → 亂碼 → 改用 Python urllib 驗中文 API。
- **狀態** 部署完成、API 驗收通過。
- **待辦** 接 Open LLM VTuber。

---

## 2026-06-01 — 第 ⑤ 步：合併 + 量化 Q4_K_M

- **做了什麼** 合併 LoRA → 16bit → `convert_hf_to_gguf` → f16 → `llama-quantize` Q4_K_M；`llama-cli` 實測繁中正常。
- **決策** 量化選 Q4_K_M（2.33GB，8GB 可即時推論）；llama.cpp pin b9442 cuda-13.3。
- **踩雷** 合併 OOM（fp16 9GB 超 8GB）→ 改 **4-bit 載入基底**；llama-cli b9442 用 `-st`；cp950 看中文要 `chcp 65001` + `-f`。
- **狀態** 成品 `gguf/qwen3-486-q4km.gguf`。
- **待辦** Ollama 部署。

---

## 2026-05-31 — 完整 QLoRA 訓練

- **做了什麼** 跑完整 2-epoch QLoRA（146 筆 × 2 ÷ 有效 batch 8 ≈ 38 step），LoRA 存 `train_outputs/lora/`（r=8/alpha=16）。
- **決策** 超參：max_seq 1024、lr 1e-4 cosine、grad accum 8、adamw_8bit、bf16。
- **踩雷** cp950 主控台遇 Unsloth emoji 崩潰 → 腳本強制 UTF-8 stdout。
- **狀態** 訓練完成、adapter 人工測試有角色感。
- **待辦** 合併量化。

---

## 2026-05-31 — 訓練腳本 TDD 對齊

- **做了什麼** 以 TDD 重寫 `train_qwen3_486.py`，拆 `build_datasets`/`make_sft_config`/`load_model_and_tokenizer`/`main` 可測單元 + pytest；`--smoke`（1 step）煙霧測試綠燈。
- **決策** 對齊實際 trl 0.19.1：`processing_class=`、欄位移到 SFTConfig。
- **踩雷** 舊腳本用 `tokenizer=`/`dataset_text_field` 建構式 → API 不符。
- **狀態** pytest 3 passed、smoke 綠。
- **待辦** 開完整訓練。

---

## 2026-05-29 — 環境建置 + 資料切分

- **做了什麼** uv + Python 3.12 建 venv；裝 GPU torch / Unsloth / Ollama / llama.cpp；`verify_env.py` 紅燈→綠燈；資料 163 筆 UTF-8 驗證、切 146/17。
- **決策** verify_env.py 當 exit-0 環境閘門。
- **踩雷** CUDA 13.2 無 bitsandbytes 預編譯 binary → 退到 cu128 torch wheel。
- **狀態** 環境綠燈（CUDA True、torch 2.11.0+cu128、UNSLOTH OK）。
- **待辦** 寫訓練腳本。

---

## 2026-05-29 — 規劃文件 + 專案遷移

- **做了什麼** 完成 PROJECT_ARCHITECTURE / FINETUNING_DETAIL_REPORT / EXECUTION_RUNBOOK；git init；專案遷移到單一根目錄 `D:\AI_486`，對齊 OLV quick-start。
- **決策** 主模型 Qwen3-4B、QLoRA、繁中、8GB GPU；資料只學語氣不灌知識。
- **踩雷** —
- **狀態** 規劃定案、git 開始追蹤。
- **待辦** 建環境。

---

## 2026-05-26 — 研讀 Ollama 文檔

- **做了什麼** 讀 Ollama 文檔：本地模型部署、Modelfile（FROM/SYSTEM/參數）、OpenAI 相容 API、模型保留/卸載。
- **決策** 部署層用 Ollama（OpenAI 相容，最易被 OLV 呼叫）。
- **踩雷** —
- **狀態** 技術路線整合完成：Unsloth → GGUF → Ollama → OLV。
- **待辦** 寫規劃文件、動工。

---

## 2026-05-19 — 研讀 Unsloth 文檔

- **做了什麼** 讀 Unsloth 文檔：QLoRA 微調流程、4-bit/低 VRAM 優化、gradient checkpointing、與 trl/transformers 搭配。
- **決策** 微調用 Unsloth（單卡、低 VRAM、Windows 友善），非 LLaMA-Factory。
- **踩雷** —
- **狀態** 確認 8GB 可走「小模型 + QLoRA + 量化」。
- **待辦** 讀 Ollama 文檔。

---

## 2026-05-12 — 研讀 OLV 文檔（Live2D / TTS / ASR）

- **做了什麼** 讀 Open LLM VTuber：Live2D 模型註冊（model_dict.json）、emotion → 表情映射、TTS/ASR 引擎選項與設定。
- **決策** Live2D 走官方文件路線；TTS 先用輕量引擎。
- **踩雷** —
- **狀態** 串接細節有概念。
- **待辦** 讀 Unsloth 文檔。

---

## 2026-05-05 — 研讀 OLV 文檔（架構與串接）

- **做了什麼** 讀 Open LLM VTuber：整體架構、FastAPI + WebSocket、LLM 透過 OpenAI 相容 API 串接、conf.yaml 設定結構。
- **決策** VTuber 框架選 Open LLM VTuber。
- **踩雷** —
- **狀態** 對整體技術棧建立全貌。
- **待辦** 續讀 OLV 多模態部分。

---

## 2026-04-28 — 資料清理與盤點

- **做了什麼** 清理素材（去空白/亂碼/重複/過長）；盤點規模約 163 筆；定 train/eval 切分方向。
- **決策** 控制資料量避免過擬合；定位「語氣微調」非知識灌輸。
- **踩雷** —
- **狀態** 資料集雛形完成。
- **待辦** 進入技術文檔研讀階段。

---

## 2026-04-21 — 整理格式 + emotion 標註

- **做了什麼** 把蒐集的對話規整成 chat `messages`（system/user/assistant）；於 assistant 開頭標 `[emotion]`。
- **決策** emotion 固定 8 類（neutral/joy/sadness/anger/surprise/smirk/fear/disgust）。
- **踩雷** —
- **狀態** 格式統一、標籤就位。
- **待辦** 清理與盤點。

---

## 2026-04-14 — 蒐集角色語料

- **做了什麼** 「下個禮拜」啟動：蒐集菜月昴角色對話/語氣素材，作為微調資料來源。
- **決策** 聚焦語氣與情緒節奏，不堆原作長台詞（版權 + 過擬合考量）。
- **踩雷** —
- **狀態** 素材初步成形。
- **待辦** 整理格式、標 emotion。

---

## 2026-04-07 — 專案起點：確定做 RE:0 微調

- **做了什麼** 拍板專案題材：做一個以《Re:0》菜月昴為風格的本地 AI VTuber，核心是對小型語言模型做角色微調；定**非官方二創、私人學習**定位。
- **決策** 繁體中文為主；先求完整流程可運作而非最大模型；不宣稱官方授權、不做未授權聲線克隆。
- **踩雷** —
- **狀態** 專案方向定案，正式啟動。
- **待辦** 下週開始蒐集資料。

---

# 檢討報告（時間分配 Retrospective）

> 定位：**工程檢討、對內**。聚焦本專案最大的問題 —— **時間分配失衡**。
> 每條一律「**問題 → 根因 → 未來解法**」。寫給下一個專案的自己看。

## 0. 先看時間軸：前置 7 週 vs 實作 4 天

把 WORKLOG 條目按階段攤平，時間分配一眼失衡：

| 期間 | 長度 | 階段 | 內容 |
| --- | ---: | --- | --- |
| 04-07 ~ 04-28 | **~3 週** | 資料 | 蒐集 → 格式 → emotion 標註 → 清理（163 筆） |
| 05-05 ~ 05-26 | **~4 週** | 研讀文檔 | OLV 架構 → OLV 多模態 → Unsloth → Ollama（每週一篇） |
| 05-29 | 1 天 | 規劃＋環境 | 規劃文件、專案遷移、建 venv、資料切分 |
| 05-31 | 1 天 | 訓練 | TDD 腳本＋完整 QLoRA（≈38 step） |
| 06-01 | **1 天** | 實作大爆發 | 合併量化 → 部署 → 串接三階段 → TTS（**一天 5 條目**） |
| 06-02 | 1 天 | 報告 | REPORT.md |

**前置（資料＋讀文檔）約 7 週、佔全程 ~78%；真正的端到端實作（環境→訓練→量化→部署→串接）全壓在 05-29 ~ 06-01 約 4 天爆發。** 這個「前面太鬆、後面太趕」就是本檢討的核心。

---

## 1. 前置過重、實作壓尾（最核心）

- **問題**：7 週前置 vs 4 天實作。所有整合雷（合併 OOM、API 亂碼、簡體覆寫、表情不動、TTS 無聲）全擠在 06-01 同一天爆。
- **根因**：採**瀑布式**——「資料做完 → 文檔讀完 → 才開始動手」。整合風險被一路推到最後才暴露，而整合雷越晚撞、定位成本越高（06-01 一天連環爆就是代價）。
- **未來解法**：改**垂直切片（vertical slice）**。專案第一週就用最小資料（哪怕 10 筆）打通一條 `env → train → quantize → deploy → 串接` 的 happy path，**把 ffmpeg / Cubism 相容 / 編碼這些整合雷在第一週就撞掉**，之後再回頭加資料、深調。先求「端到端會動」，再求「每段做好」。

## 2. 研讀文檔過度前置、線性堆疊

- **問題**：05-05 ~ 05-26 整整 4 週，每週讀一個工具文檔（OLV→OLV→Unsloth→Ollama），但這些知識到 05-29 之後才真正動用。
- **根因**：「**全部讀完才動工**」的心態。讀完到用之間隔數週 → 學了會忘、用時重查，等於白讀一輪；且 4 週只有輸入、沒有產出，動能極低。
- **未來解法**：**邊做邊學取代先讀後做**。把文檔研讀**分散綁到各實作階段**（要用 Unsloth 那週才深讀 Unsloth）。每個工具給 **timebox（如 2 天）**，到點就動手跑最小範例驗證，用「做」來固化「讀」，避免讀到飽才開工。

## 3. 早期節奏是「週更」而非「持續推進」

- **問題**：04-07 ~ 05-26 每個條目剛好間隔 7 天（07/14/21/28/05/12/19/26），明顯是「一週碰一次」。真正進入狀態、天天推進是 05-29 之後。
- **根因**：前期沒有**固定產出節奏與里程碑壓力**，靠零散的「有空再碰」推進，動能低、上下文每次都要重建。
- **未來解法**：設**短週期里程碑＋固定節奏**（如每 2~3 天一個可驗收的小目標），讓專案保持動能、減少「重新熱機」的成本。把前 7 週鬆散的時間，挪一部分到後段整合與驗收。

## 4. 資料花 3 週、品質問題卻沒在資料階段攔下

- **問題**：花約 3 週整理 163 筆資料（訓練本身只跑半天 / 38 step），但**情緒偏態（fear 偏多）**這種品質問題，拖到寫 REPORT 才發現。
- **根因**：3 週時間花在「蒐集／格式／標註／清理」，**卻沒有一步是「資料品質量測」**——沒做 emotion 分布統計、沒做抽樣評估設計。時間花了，但沒花在刀口上。
- **未來解法**：在資料階段就加**品質閘門**：跑 emotion 分布統計＋抽樣人工檢查，把偏態在資料階段攔掉；同時**提早設計評估**（固定測試題、base vs after A/B），別等到報告階段才補。

## 5. 資料一路做到「完美」才微調，中間沒有早期測試回饋

- **問題**：花 3 週把 163 筆資料蒐集、標註、清理「做完」，**才第一次真正拿去微調**。整個資料階段從沒跑過一次模型來看「學出來長怎樣」。
- **根因**：把資料當成**一次性交付**而非**可迭代產物**——資料與模型之間沒有 feedback loop。結果情緒偏態、語氣是否吃得進去、emotion 標籤學不學得會，全都得等到資料「完工」後才知道，問題暴露得太晚。
- **未來解法**：**資料早期就先微調一次當探針**。哪怕只有幾十筆、只跑幾個 step，先看模型實機輸出，用結果反推「資料哪裡要補、標註怎麼改、微調方式（r/epoch/資料配比）要不要調」，**迭代個 2~3 輪**再放大資料量。這正是 §1 垂直切片在「訓練」這一刀的具體做法——**用最便宜的一次微調，提早暴露資料與方法的問題**，而不是把 3 週賭在「資料先做對」上。

## 6. 環境閘門只顧訓練、不顧串接（時間分配的延伸）

- **問題**：`verify_env.py` 只驗訓練端（GPU/torch/unsloth），沒涵蓋串接端隱性依賴（**ffmpeg**、ollama、前端 submodule）。最關鍵的「無聲」雷因此拖到 06-01 最後才爆。
- **根因**：把「環境」窄化成「能不能訓練」，沒把「能不能跑完整條互動鏈」一起納入閘門 —— 本質還是**整合驗證被排到最後**。
- **未來解法**：環境閘門**一次涵蓋全鏈**（訓練＋部署＋串接的所有外部依賴），呼應 §1 的垂直切片：第一週打通 happy path 時，這些依賴自然就被閘門攔出來。

---

## 7. 一句話總結

> **本專案不是「做不出來」，而是「太晚開始做」**：7 週前置把所有整合風險推到最後 4 天引爆。下一個專案的鐵律 —— **第一週就讓端到端動起來，再回頭把每段做好。**

## 8. 也有做對的（時間面）

- TDD 先對齊 trl API（05-31），避免訓練到一半才發現 API 不符、整批重跑 —— 這是把風險前移做對的一次。
WebSocket 探針）省下重複試錯的時間。
- 真正動手後 4 天就端到端打通，證明**實作密度其實夠高**——問題從來不是能力，而是太晚把這份密度投進去。
